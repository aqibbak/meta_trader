$(document).ready(function() {
    /* TODO: finishme! */

    /* Init */
    // hide_all_tabs();
    // $('.common_tab').show();

    var hide_all_tabs = function() {
        $('.tab').each(function(index, item) { $(item).hide(); });
    }
});
